<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hmh.repository.RoutineCycleMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="seqNo" keyColumn="seq_no">
        INSERT INTO routine_cycle
        (routine_seq_no, member_seq_no, cycle_number, start_ymd, end_ymd, target_value)
        VALUES(#{routineSeqNo}, #{memberSeqNo}, #{cycleNumber}, #{startYmd}, #{endYmd}, #{targetValue})
    </insert>

    <update id="update" parameterType="RoutineCycle">
        UPDATE routine_cycle
        SET target_value = #{targetValue},
            current_achieved_value = #{currentAchievedValue}
        WHERE
            seq_no = #{seqNo}
    </update>


</mapper>